declare const enum Direction {
    //% block=left
    Left,
    //% block=right
    Right
}

/**
 * Enum for MASKS
 */
declare const enum maskType {
    ironman,
    average,
    monalisa,
    sean_connery,
    skull,
    audrey,
    nicolas_cage
}
